using System;
using System.Collections.Generic;
using System.Linq;

namespace LINQLibrary
{
    public static class Class1
    {
    //    public static T First<T>(this IEnumerable<T> source)
    //    {
    //        return source.First<T>();
    //    }
    //    public static T Last<T>(this IEnumerable<T> source) { return source.Last<T>(); }

    //    public static IEnumerable<T> Where<T>(this IEnumerable<T> source, Func<T, bool> predicate)
    //    {
    //        return source.Where(predicate);
    //    }
    //    public static IEnumerable<T> Take<T>(this IEnumerable<T> source, int count) { return source.Take(count); }

    //    public static IOrderedEnumerable<T> OrderBy<T, Key>(this IEnumerable<T> source, Func<T, Key> keySeleeeeeeeeector)
    //    {
    //        return source.OrderBy(keySeleeeeeeeeector);
    //    }

    //    public static IEnumerable<TResult> Select<T, TResult>(this IEnumerable<T> sourceeee, Func<T, TResult> selector)
    //    {
    //        return sourceeee.Select(selector);
    //    }

    
    }
}
